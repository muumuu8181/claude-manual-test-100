タスク: 簡単な計算ツールの作成と実行

以下の手順を順番通りに実行してください。各手順は前の結果を使用して次のプログラムを作成します。

1. 作業用ディレクトリ「calculator_project」を作成する
2. calculator_project内に「src」フォルダを作成する
3. calculator_project内に「data」フォルダを作成する
4. calculator_project内に「results」フォルダを作成する
5. srcフォルダ内に「basic_math.py」ファイルを作成し、以下のPythonコードを書き込む：
   ```python
   def add_numbers(a, b):
       return a + b
   
   def multiply_numbers(a, b):
       return a * b
   
   def calculate_average(numbers):
       return sum(numbers) / len(numbers)
   ```
6. srcフォルダ内に「file_processor.py」ファイルを作成し、以下のコードを書き込む：
   ```python
   def read_numbers_from_file(filename):
       with open(filename, 'r') as f:
           return [int(line.strip()) for line in f if line.strip()]
   
   def write_result_to_file(filename, result):
       with open(filename, 'w') as f:
           f.write(str(result))
   ```
7. dataフォルダ内に「input_A.txt」ファイルを作成し、以下の数値を1行ずつ書き込む：
   「15\n25\n35\n45\n55」
8. dataフォルダ内に「input_B.txt」ファイルを作成し、以下の数値を1行ずつ書き込む：
   「10\n20\n30\n40\n50」
9. srcフォルダ内に「calculator_main.py」ファイルを作成し、以下のコードを書き込む：
   ```python
   from basic_math import add_numbers, multiply_numbers, calculate_average
   from file_processor import read_numbers_from_file, write_result_to_file
   
   # データファイルを読み込み
   numbers_a = read_numbers_from_file('../data/input_A.txt')
   numbers_b = read_numbers_from_file('../data/input_B.txt')
   
   # 計算実行
   sum_a = sum(numbers_a)
   sum_b = sum(numbers_b)
   total_sum = add_numbers(sum_a, sum_b)
   average_a = calculate_average(numbers_a)
   average_b = calculate_average(numbers_b)
   product_of_averages = multiply_numbers(average_a, average_b)
   
   # 結果をファイルに保存
   write_result_to_file('../results/sum_a.txt', sum_a)
   write_result_to_file('../results/sum_b.txt', sum_b)
   write_result_to_file('../results/total_sum.txt', total_sum)
   write_result_to_file('../results/average_a.txt', average_a)
   write_result_to_file('../results/average_b.txt', average_b)
   write_result_to_file('../results/product_averages.txt', product_of_averages)
   
   print("計算完了: 結果はresultsフォルダに保存されました")
   ```
10. srcディレクトリに移動し、calculator_main.pyを実行する
11. 実行後、resultsフォルダ内の「sum_a.txt」の内容を確認し、正しい値（175）が保存されていることを検証する
12. resultsフォルダ内の「sum_b.txt」の内容を確認し、正しい値（150）が保存されていることを検証する
13. resultsフォルダ内の「total_sum.txt」の内容を確認し、正しい値（325）が保存されていることを検証する
14. resultsフォルダ内の「average_a.txt」の内容を確認し、正しい値（35.0）が保存されていることを検証する
15. resultsフォルダ内の「average_b.txt」の内容を確認し、正しい値（30.0）が保存されていることを検証する
16. resultsフォルダ内の「product_averages.txt」の内容を確認し、正しい値（1050.0）が保存されていることを検証する
17. calculator_projectのルートディレクトリに「execution_summary.txt」ファイルを作成し、以下の内容を書き込む：
    「Calculation Summary\nInput A Sum: [sum_a.txtの値]\nInput B Sum: [sum_b.txtの値]\nTotal Sum: [total_sum.txtの値]\nExecution Status: Success」
18. 全ファイルが正しく作成され、プログラムが正常に実行されたことを最終確認する